import chess.ChessBoardImpl;
import chess.ChessMoveImpl;
import chess.ChessPositionImpl;

class PrintLN {
    public static void main(String[] args)
    {
        /*
        ChessBoardImpl board=new ChessBoardImpl();
        board.resetBoard();
        System.out.println(board.toStringBoard()+"\n");
        ChessMoveImpl newMove=new ChessMoveImpl(new ChessPositionImpl(1,1),new ChessPositionImpl(3,3),null);
        board.movePiece(newMove);
        System.out.println(board.toStringBoard()+"\n");
        System.out.println(board.getPiece(newMove.getEndPosition()).toStringAllMoves());

         */
    }
}
